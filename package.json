{
  "name": "cloudformation-lambda-ddb-example-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "aws cloudformation package --template-file template.yaml --s3-bucket koutsopoulosw-deployment --output-template-file outputSamTemplate.yml --s3-prefix cloudformation-lambda-ddb-example-api --region us-east-1",
    "deploy": "aws cloudformation deploy --template-file outputSamTemplate.yml --stack-name REDACTED --capabilities CAPABILITY_NAMED_IAM --parameter-overrides file://cfparameters.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1692.0"
  }
}
